 function Todo(todoId, 
    name, isDone, createdDate, updatedDate, ownerId) {
    this.todoId = todoId;
    this.name = name;
    this.isDone = isDone;
    this.createdDate = createdDate;
    this.updatedDate = updatedDate;
    this.ownerId = ownerId;
}
